import { FontSwitcherDirective } from './font-switcher.directive';

describe('FontSwitcherDirective', () => {
  it('should create an instance', () => {
    const directive = new FontSwitcherDirective();
    expect(directive).toBeTruthy();
  });
});
